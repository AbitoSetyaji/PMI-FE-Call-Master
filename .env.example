# API Configuration
# Backend API URL - Do NOT include /api prefix or trailing slash
NEXT_PUBLIC_API_URL=http://148.230.100.61:8000

# Note: When deploying to Hostinger VPS with Docker,
# update NEXT_PUBLIC_API_URL to your actual backend URL
# The backend routes are configured at root level (e.g., /auth/login, not /api/auth/login)
